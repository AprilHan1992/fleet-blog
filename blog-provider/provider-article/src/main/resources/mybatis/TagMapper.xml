<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fleet.provider.article.dao.TagDao">
    <resultMap id="BaseResultMap" type="com.fleet.common.entity.article.Tag">
        <id column="tag_id" property="tagId"/>
        <result column="tag_type" property="tagType"/>
        <result column="tag" property="tag"/>
        <result column="sort_value" property="sortValue"/>
        <result column="upper_id" property="upperId"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
		`tag_id`, `tag_type`, `tag`, `sort_value`, `upper_id`, `deleted`
	</sql>

    <insert id="insert" parameterType="com.fleet.common.entity.article.Tag" useGeneratedKeys="true" keyProperty="tagId">
        insert into `tag` (
        <trim suffixOverrides=",">
            <if test="tagType != null">`tag_type`,</if>
            <if test="tag != null">`tag`,</if>
            <if test="sortValue != null">`sort_value`,</if>
            <if test="upperId != null">`upper_id`,</if>
            <if test="deleted != null">`deleted`,</if>
        </trim>
        ) values (
        <trim suffixOverrides=",">
            <if test="tagType != null">#{tagType},</if>
            <if test="tag != null">#{tag},</if>
            <if test="sortValue != null">#{sortValue},</if>
            <if test="upperId != null">#{upperId},</if>
            <if test="deleted != null">#{deleted},</if>
        </trim>
        )
    </insert>

    <update id="delete" parameterType="com.fleet.common.entity.article.Tag">
        update `tag` set `deleted` = 1
        <where>
            <if test="tagId != null">and `tag_id` = #{tagId}</if>
            <if test="tagType != null">and `tag_type` = #{tagType}</if>
            <if test="tag != null">and `tag` = #{tag}</if>
            <if test="sortValue != null">and `sort_value` = #{sortValue}</if>
            <if test="upperId != null">and `upper_id` = #{upperId}</if>
        </where>
    </update>

    <update id="update" parameterType="com.fleet.common.entity.article.Tag">
        update `role`
        <set>
            <if test="tagType != null">`tag_type` = #{tagType},</if>
            <if test="tag != null">`tag` = #{tag},</if>
            <if test="sortValue != null">`sort_value` = #{sortValue},</if>
            <if test="upperId != null">`upper_id` = #{upperId},</if>
            <if test="deleted != null">`deleted` = #{deleted},</if>
        </set>
        where tag_id = #{tagId}
    </update>

    <select id="get" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `tag`
        <where>
            <if test="tagId != null">and `tag_id` = #{tagId}</if>
            <if test="tagType != null">and `tag_type` = #{tagType}</if>
            <if test="tag != null">and `tag` = #{tag}</if>
            <if test="sortValue != null">and `sort_value` = #{sortValue}</if>
            <if test="upperId != null">and `upper_id` = #{upperId}</if>
            <if test="deleted != null">and `deleted` = #{deleted}</if>
            <if test="deleted == null">and `deleted` = 0</if>
        </where>
        limit 1
    </select>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `tag`
        <where>
            <if test="tagId != null">and `tag_id` = #{tagId}</if>
            <if test="tagType != null">and `tag_type` = #{tagType}</if>
            <if test="tag != null">and `tag` = #{tag}</if>
            <if test="sortValue != null">and `sort_value` = #{sortValue}</if>
            <if test="upperId != null">and `upper_id` = #{upperId}</if>
            <if test="deleted != null">and `deleted` = #{deleted}</if>
            <if test="deleted == null">and `deleted` = 0</if>
        </where>
    </select>

    <select id="tagIdList" resultType="java.lang.Integer">
        select `tag_id` from `tag`
        <where>
            <if test="tagId != null">and `tag_id` = #{tagId}</if>
            <if test="tagType != null">and `tag_type` = #{tagType}</if>
            <if test="tag != null">and `tag` = #{tag}</if>
            <if test="sortValue != null">and `sort_value` = #{sortValue}</if>
            <if test="upperId != null">and `upper_id` = #{upperId}</if>
            <if test="deleted != null">and `deleted` = #{deleted}</if>
            <if test="deleted == null">and `deleted` = 0</if>
        </where>
    </select>
</mapper>
